<Page x:Class="TicTacToe3D.Pages.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TicTacToe3D.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="640" d:DesignWidth="960"
      Loaded="GamePage_OnLoaded"
      Unloaded="GamePage_OnUnloaded"
      Title="GamePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Canvas 
            Name="GameCanvas"
            ClipToBounds="True"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            Background="Black"
            Grid.Column="0">
            
<!--            <Polygon Fill="Red" Points="10,10 100,100 100,10 10,100"></Polygon>-->
        </Canvas>


        <StackPanel
            Background="Red"
            Grid.Column="1"
            Orientation="Vertical">
            <TextBlock
                Text="Testowy tekst"/>
            <Button
                Background="GreenYellow"
                Content="Testowy guzik"/>
        </StackPanel>
    </Grid>
</Page>

